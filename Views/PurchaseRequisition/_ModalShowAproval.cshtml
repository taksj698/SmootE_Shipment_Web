@model QuickVisualWebWood.Core.pageModels.PurchaseRequisition.ModalShowApproval
<div class="modal fade" id="positionApprove" tabindex="-1" aria-modal="true" role="dialog">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title">ผู้อนุมัติ</h5>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
			</div>
			<div class="modal-body">
				<table class="table table-bordered">
					<tbody>
						<tr>
							<th scope="col">ชื่อ-นามสกุล</th>
							<th scope="col">ติดต่อ</th>
						</tr>
					</tbody>
					<tbody>
						@if (Model != null && Model.approvalDetails != null && Model.approvalDetails.Count > 0)
						{
							foreach (var item in Model.approvalDetails)
							{
								<tr>
									<td>@item.Name</td>
									<td>@item.TelNo</td>
								</tr>
							}
						}
						else
						{
							<tr>
								<td colspan="2" class="text-center">ไม่พบข้อมูล</td>
							</tr>
						}
					</tbody>
				</table>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ปิด</button>
			</div>
		</div>
	</div>
</div>